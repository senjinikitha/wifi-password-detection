# wifi-password-detection
This Python script lists all previously connected Wi-Fi network names along with their saved passwords on a Windows system. It uses the `subprocess` module to execute `netsh` commands and extract stored credentials.
